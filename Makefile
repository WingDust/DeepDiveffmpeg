# 终极目标
t:t.c
	@gcc -o t t.c
	# 加上 @ 可以不打印在运行这条命令时不将这个命令打印出来，默认是打印的

# 变量
app = 我
# 通过 $() 或 ${} 来引用变量
# echo $(app)

# % 匹配 0 个或多个字符，可理解为匹配文件名
# × 可用于匹配单个文件名
# ?= 条件赋值，如果没有实赋值，将会被赋值
# += 等同javascript 
# 自动变量




# 来指定clean为伪目标，这样make就总会执行该目标的命令
.PHONY: clean
clean:
	rm -rf *.o t